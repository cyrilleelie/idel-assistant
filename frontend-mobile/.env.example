# Copier en .env et renseigner les valeurs (jamais commiter .env)

# URL de l'API backend, accessible depuis l'appareil/émulateur mobile
# (utiliser l'IP locale de la machine hôte, pas localhost, pour un device physique)
EXPO_PUBLIC_API_URL=http://localhost:8000/api/v1
